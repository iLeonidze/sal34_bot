version: '3'
services:
  app:
    image: ghcr.io/ileonidze/sal34_bot:latest
    restart: always
    volumes:
      - ./buildings:/opt/bot/buildings
      - ./configs:/opt/bot/configs
      - ./data:/opt/bot/data
      - ./stats:/opt/bot/stats
      - ./users:/opt/bot/users
